[project]
name = "lumen-concurrency"
version = "0.1.0"
description = "Concurrency test for Lumen"
dependencies = [
    "httpx",
    "numpy",
    "matplotlib",
    "tabulate",
]

[tool.uv]
managed = true
package = false
